-- Placeholder: this version was applied directly to the remote database
-- (outside this migrations folder) before it was tracked here. No-op —
-- exists only so `supabase migration repair` can record the history entry
-- without re-running unknown SQL against a database that already has it.
select 1;
